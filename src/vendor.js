/*
    This file contains references to the vendor libraries 
    we're using in this project. This is used by webpack
    in the production build only. This is so that changes
    to our production JS will not require customers to 
    re-download all vendor libraries when it is not needed.
*/

/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';